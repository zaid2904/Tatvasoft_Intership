import { Routes } from '@angular/router';
import { MyFormComponent } from './my-form/my-form.component'; // adjust path

export const routes: Routes = [
  { path: '', component: MyFormComponent },
];

